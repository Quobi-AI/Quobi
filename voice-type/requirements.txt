sounddevice==0.5.1
pynput==1.7.7
requests==2.32.3
numpy==1.26.4
pystray==0.19.5
pillow==11.0.0
# evdev is the Linux/Wayland hotkey backend (grab+replay). Linux-only — the
# marker makes pip skip it on Windows/macOS, which use the pynput backend.
evdev==1.7.1 ; sys_platform == "linux"
# On-device transcription (local STT engine): NVIDIA Parakeet via sherpa-onnx,
# run in-process on CPU (ONNX Runtime). No CUDA, no sidecar, same code path on
# Linux + Windows. The ONNX model bundle downloads on first run. sherpa-onnx
# bundles its own onnxruntime, so there's no separate ASR runtime dependency.
sherpa-onnx==1.13.3
setuptools<81
